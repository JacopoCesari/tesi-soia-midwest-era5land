# Spatial weights

The supplied 479-county matrix has 11,953 intersections. CSV and Parquet bytes are preserved. Recompute into a separate directory; readers normalize lat/lon to latitude/longitude internally.
