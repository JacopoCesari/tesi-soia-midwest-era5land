# Experiments

Future model experiments go under ignored runs/<experiment_name>/. Each run should record configuration, inputs, code revision, fold metadata and metrics. Pipeline QC JSON goes under ignored quality_control/.
